function footer() {
  return ( <div id="footer" style={{display:'flex',background:'red'}}>
      <nav>
        <span>Hourly</span>
        <span>Dailt</span>
      </nav>
  </div> );
}

export default footer;